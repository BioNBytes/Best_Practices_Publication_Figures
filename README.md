# Best Practices Publication Figures


## Setting R/python for consistent ggplots, complexheatmaps, sns, matplotlib

define theme in R, define heatmap params, set fontsize, font text

define global settings for sns/matplotlib for same things in python


Always save figures as pdf vector images, these can be manipulated in adobe illustrator
note raster images are not vector images and cannot


## Adobe illustrator e.g. of figure generation
Font size, color picker for copying format (color, line size/dash style)
- drag drop pdf, embed, cmd A (select all), cmd opt 7 (release mask all), opt shift g (ungroup all)
